# TODO

- which-key bindings for LSP actions (especially rename variable)
  - <leader>cr
- close quickfix list keybinding
  - so far this really hasn't come up
- gl for floating diagnostic text?
  - trouble.nvim <leader>xx 
  - <leader>cd
  - I still like gl
- some key binding that opens the config
  - <leader>fc
- is set spell on by default?
- fix explorer so reopening uses cwd from file?
- remap delete buffers to left and delete buffers to right to use h and l

## Remove from which-key

- LazyVim change log
- Split buffer below
- Switch to other buffer
- Split window right
